import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import VRFitApp from "./vrfit";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <VRFitApp />
    </React.StrictMode>
);
